name: One-shot tree and drop parity fix

on:
  push:
    branches: [agent/tree-range-drop-fix]
    paths: [.github/workflows/tree-drop-fix.yml]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/tree-range-drop-fix
          fetch-depth: 0
      - name: Restore legacy tree scan and item throw behavior
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          manager = Path('src/main/java/atomicstryker/minions/common/MinionManager.java')
          s = manager.read_text()
          old = '''    public static boolean chopTree(ServerPlayer player, BlockPos start) {
        ServerLevel level = player.serverLevel();
        if (!level.getBlockState(start).is(BlockTags.LOGS)) {
            return false;
        }
        List<BlockPos> work = floodMatching(level, start, MinionsConfig.MAX_TREE_BLOCKS.get(), true);
        assignBreakWork(player, work);
        if (!work.isEmpty()) {
            playOrder(player, MinionsSounds.ORDER_TREE_CUTTING.get());
            exhaustBig(player);
            return true;
        }
        return false;
    }
'''
          new = '''    public static boolean chopTree(ServerPlayer player, BlockPos start) {
        ServerLevel level = player.serverLevel();
        if (!level.getBlockState(start).is(BlockTags.LOGS)) {
            return false;
        }

        List<List<BlockPos>> trees = scanNearbyTrees(level, start);
        assignTreeWork(player, trees);
        if (!trees.isEmpty()) {
            playOrder(player, MinionsSounds.ORDER_TREE_CUTTING.get());
            exhaustBig(player);
            return true;
        }
        return false;
    }

    /**
     * Legacy TreeScanner parity: one clicked tree starts a local harvest job which
     * searches outward for up to sixteen trees, out to sixty-four blocks.
     */
    private static List<List<BlockPos>> scanNearbyTrees(ServerLevel level, BlockPos start) {
        final int searchRadius = 64;
        final int maxTrees = 16;
        final int maxBlocksPerTree = Math.max(64, MinionsConfig.MAX_TREE_BLOCKS.get());
        List<List<BlockPos>> trees = new ArrayList<>();
        Set<BlockPos> claimedLogs = new HashSet<>();

        BlockPos clickedBase = start.immutable();
        while (clickedBase.getY() > level.getMinBuildHeight() && level.getBlockState(clickedBase.below()).is(BlockTags.LOGS)) {
            clickedBase = clickedBase.below();
        }
        tryAddTree(level, clickedBase, maxBlocksPerTree, trees, claimedLogs, maxTrees);

        int centerX = start.getX();
        int centerZ = start.getZ();
        for (int radius = 1; radius <= searchRadius && trees.size() < maxTrees; radius++) {
            for (int dx = -radius; dx <= radius && trees.size() < maxTrees; dx++) {
                tryAddTreeColumn(level, centerX + dx, centerZ - radius, maxBlocksPerTree, trees, claimedLogs, maxTrees);
                if (radius != 0) {
                    tryAddTreeColumn(level, centerX + dx, centerZ + radius, maxBlocksPerTree, trees, claimedLogs, maxTrees);
                }
            }
            for (int dz = -radius + 1; dz <= radius - 1 && trees.size() < maxTrees; dz++) {
                tryAddTreeColumn(level, centerX - radius, centerZ + dz, maxBlocksPerTree, trees, claimedLogs, maxTrees);
                tryAddTreeColumn(level, centerX + radius, centerZ + dz, maxBlocksPerTree, trees, claimedLogs, maxTrees);
            }
        }
        return trees;
    }

    private static void tryAddTreeColumn(ServerLevel level, int x, int z, int maxBlocksPerTree,
                                         List<List<BlockPos>> trees, Set<BlockPos> claimedLogs, int maxTrees) {
        if (trees.size() >= maxTrees) {
            return;
        }
        int topY = level.getHeight(net.minecraft.world.level.levelgen.Heightmap.Types.MOTION_BLOCKING_NO_LEAVES, x, z);
        int bottomY = Math.max(level.getMinBuildHeight(), topY - 48);
        for (int y = topY; y >= bottomY; y--) {
            BlockPos pos = new BlockPos(x, y, z);
            if (!level.getBlockState(pos).is(BlockTags.LOGS)) {
                continue;
            }
            while (pos.getY() > level.getMinBuildHeight() && level.getBlockState(pos.below()).is(BlockTags.LOGS)) {
                pos = pos.below();
            }
            tryAddTree(level, pos, maxBlocksPerTree, trees, claimedLogs, maxTrees);
            return;
        }
    }

    private static void tryAddTree(ServerLevel level, BlockPos base, int maxBlocksPerTree,
                                   List<List<BlockPos>> trees, Set<BlockPos> claimedLogs, int maxTrees) {
        if (trees.size() >= maxTrees || claimedLogs.contains(base) || !level.getBlockState(base).is(BlockTags.LOGS)) {
            return;
        }
        List<BlockPos> tree = collectTreeLogs(level, base, maxBlocksPerTree);
        if (tree.size() <= 3) {
            return;
        }
        claimedLogs.addAll(tree);
        tree.sort(Comparator.comparingInt(BlockPos::getY));
        trees.add(tree);
    }

    private static List<BlockPos> collectTreeLogs(ServerLevel level, BlockPos base, int limit) {
        Queue<BlockPos> open = new ArrayDeque<>();
        Set<BlockPos> seen = new HashSet<>();
        List<BlockPos> logs = new ArrayList<>();
        open.add(base.immutable());

        while (!open.isEmpty() && logs.size() < limit) {
            BlockPos pos = open.remove();
            if (!seen.add(pos) || !level.getBlockState(pos).is(BlockTags.LOGS)) {
                continue;
            }
            logs.add(pos);

            // The old TreeScanner followed a 3x3 footprint at the same level and one level up.
            // This catches diagonal branches and wide/modded trunks that the beta.2 six-way flood missed.
            for (int dy = 0; dy <= 1; dy++) {
                for (int dx = -1; dx <= 1; dx++) {
                    for (int dz = -1; dz <= 1; dz++) {
                        if (dx == 0 && dy == 0 && dz == 0) {
                            continue;
                        }
                        BlockPos next = pos.offset(dx, dy, dz);
                        if (!seen.contains(next)) {
                            open.add(next.immutable());
                        }
                    }
                }
            }
        }
        return logs;
    }

    private static void assignTreeWork(ServerPlayer player, List<List<BlockPos>> trees) {
        List<MinionEntity> minions = getOwned(player);
        if (minions.isEmpty()) {
            return;
        }
        for (MinionEntity minion : minions) {
            minion.clearWork();
            minion.setFollowing(false);
            minion.clearMoveTarget();
        }

        int workerIndex = 0;
        for (List<BlockPos> tree : trees) {
            MinionEntity worker = minions.get(workerIndex++ % minions.size());
            for (BlockPos log : tree) {
                worker.enqueueTreeWork(log);
            }
        }
    }
'''
          if old not in s:
              raise SystemExit('chopTree block not found')
          manager.write_text(s.replace(old, new))

          entity = Path('src/main/java/atomicstryker/minions/common/entity/MinionEntity.java')
          s = entity.read_text()
          s = s.replace('import net.minecraft.world.phys.AABB;\n', 'import net.minecraft.world.phys.AABB;\nimport net.minecraft.world.phys.Vec3;\n')
          s = s.replace('    private int pickupCooldown;\n', '    private int pickupCooldown;\n    private int pickupDisabledTicks;\n')
          s = s.replace('''    public void enqueueWork(BlockPos pos) {
        enqueue(new WorkOrder(pos.immutable(), WorkAction.BREAK, null));
    }
''', '''    public void enqueueWork(BlockPos pos) {
        enqueue(new WorkOrder(pos.immutable(), WorkAction.BREAK, null));
    }

    public void enqueueTreeWork(BlockPos pos) {
        enqueue(new WorkOrder(pos.immutable(), WorkAction.TREE_BREAK, null));
    }
''')
          old_drop = '''    public void dropPassengerAndItems() {
        ejectPassengers();
        carryTargetUUID = null;
        dropStoredItems();
    }

    public void dropStoredItems() {
        if (level().isClientSide) {
            return;
        }
        for (int slot = 0; slot < inventory.getContainerSize(); slot++) {
            ItemStack stack = inventory.removeItemNoUpdate(slot);
            if (!stack.isEmpty()) {
                spawnAtLocation(stack);
            }
        }
    }
'''
          new_drop = '''    public void dropPassengerAndItems() {
        ejectPassengers();
        carryTargetUUID = null;
        if (level() instanceof ServerLevel serverLevel) {
            ServerPlayer owner = getOwner(serverLevel);
            if (owner != null) {
                dropStoredItemsToward(owner);
                return;
            }
        }
        dropStoredItems();
    }

    /** Restores the old DropAll behavior: face the master and throw the inventory toward them. */
    public void dropStoredItemsToward(ServerPlayer player) {
        if (level().isClientSide) {
            return;
        }
        pickupDisabledTicks = 60; // legacy blockItemPickUp(): three seconds
        Vec3 source = position().add(0.0D, getBbHeight() * 0.65D, 0.0D);
        Vec3 toPlayer = player.getEyePosition().subtract(source);
        Vec3 velocity = toPlayer.lengthSqr() > 0.0001D
                ? toPlayer.normalize().scale(0.38D).add(0.0D, 0.12D, 0.0D)
                : new Vec3(0.0D, 0.18D, 0.0D);

        for (int slot = 0; slot < inventory.getContainerSize(); slot++) {
            ItemStack stack = inventory.removeItemNoUpdate(slot);
            if (stack.isEmpty()) {
                continue;
            }
            ItemEntity item = new ItemEntity(level(), source.x, source.y, source.z, stack);
            item.setDeltaMovement(velocity);
            level().addFreshEntity(item);
        }
    }

    public void dropStoredItems() {
        if (level().isClientSide) {
            return;
        }
        pickupDisabledTicks = 60;
        for (int slot = 0; slot < inventory.getContainerSize(); slot++) {
            ItemStack stack = inventory.removeItemNoUpdate(slot);
            if (!stack.isEmpty()) {
                spawnAtLocation(stack);
            }
        }
    }
'''
          if old_drop not in s:
              raise SystemExit('drop block not found')
          s = s.replace(old_drop, new_drop)
          s = s.replace('''        if (pickupCooldown-- <= 0) {
            pickupCooldown = 10;
            collectNearbyItems(serverLevel);
        }
''', '''        if (pickupDisabledTicks > 0) {
            pickupDisabledTicks--;
        } else if (pickupCooldown-- <= 0) {
            pickupCooldown = 10;
            collectNearbyItems(serverLevel);
        }
''')
          s = s.replace('''        if (order.action() == WorkAction.BREAK) {
            if (state.isAir() || state.getDestroySpeed(level, target) < 0.0F) {
''', '''        if (order.action() == WorkAction.BREAK || order.action() == WorkAction.TREE_BREAK) {
            if (state.isAir() || state.getDestroySpeed(level, target) < 0.0F) {
''')
          s = s.replace('''        double distance = distanceToSqr(target.getX() + 0.5D, target.getY() + 0.5D, target.getZ() + 0.5D);
        if (distance <= 9.0D) {
''', '''        double distance = distanceToSqr(target.getX() + 0.5D, target.getY() + 0.5D, target.getZ() + 0.5D);
        double workReachSq = order.action() == WorkAction.TREE_BREAK ? 256.0D : 9.0D;
        if (distance <= workReachSq) {
''', 1)
          s = s.replace('''            if (order.action() == WorkAction.BREAK) {
                level.destroyBlock(target, true, this);
''', '''            if (order.action() == WorkAction.BREAK || order.action() == WorkAction.TREE_BREAK) {
                level.destroyBlock(target, true, this);
''')
          s = s.replace('''            case BREAK -> null;
''', '''            case BREAK, TREE_BREAK -> null;
''')
          s = s.replace('new AABB(blockPosition()).inflate(1.5D)', 'new AABB(blockPosition()).inflate(4.5D)')
          s = s.replace('''        PLACE_STAIR,
        PLACE_TORCH
''', '''        PLACE_STAIR,
        PLACE_TORCH,
        TREE_BREAK
''')
          entity.write_text(s)

          gp = Path('gradle.properties')
          s = gp.read_text().replace('mod_version=2.0.3-1.21.1-beta.2', 'mod_version=2.0.3-1.21.1-beta.3')
          gp.write_text(s)

          readme = Path('README.md')
          s = readme.read_text().replace('`2.0.3-1.21.1-beta.2`', '`2.0.3-1.21.1-beta.3`')
          s = s.replace('- tree harvesting and vein mining', '- legacy-style tree harvesting (up to 16 nearby trees / 64-block scan) and vein mining')
          readme.write_text(s)
          PY

          rm .github/workflows/tree-drop-fix.yml
          git config user.name "meynoik"
          git config user.email "neynoi.k@gmail.com"
          git add -A
          git commit -m "Restore legacy tree harvesting and item throws [skip ci]"
          git push origin agent/tree-range-drop-fix
